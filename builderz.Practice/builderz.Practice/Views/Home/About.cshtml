@using Sitecore.Mvc
@using Sitecore.Mvc.Presentation
@model RenderingModel
@*
    @{
        var datasourceItem = RenderingContext.CurrentOrNull.Rendering.RenderingItem.DataSource;
    }
*@
@*@Html.Sitecore().Rendering("RenderingPath", new { DataSource = datasourceItem })

    @{
        var abc = RenderingContext.CurrentOrNull.Rendering.RenderingItem.DataSource;
    }*@
<!-- About Start -->

@if (Model != null && Model.PageItem != null)
{
    <div class="about wow fadeInUp" data-wow-delay="0.1s">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-5 col-md-6">
                    <div class="about-img">
                        <img src="img/about.jpg" alt="Image">
                    </div>
                </div>
                <div class="col-lg-7 col-md-6">
                    <div class="section-header text-left">
                        <p>@Html.Sitecore().Field("Title", Model.PageItem)</p>
                        <h2> @Html.Sitecore().Field("SubTitle", Model.PageItem)</h2>
                    </div>
                    <div class="about-text">
                        @Html.Sitecore().Field("Description", Model.PageItem)
                        <a class="btn" href="">Learn More</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
}

<!-- About End -->