@using Sitecore.Mvc
@using Sitecore.Mvc.Presentation
@using builderz.Practice.Model
@model SinglePostModel


@if (Model != null)
{
    <div class="single-related wow fadeInUp">
        <h2>@Html.Sitecore().Field("Main_Title", Model.Item)</h2>
        <div class="owl-carousel related-slider">
            @foreach (var item in Model.RelatedPost)
            {
                <div class="post-item">
                    <div class="post-img">
                        @item.Image
                    </div>
                    <div class="post-text">
                        <a href="">@item.TitleLink</a>
                        <div class="post-meta">
                            <p>By<a href="">@item.PostBy</a></p>
                            <p>In<a href="">@item.In</a></p>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>

}